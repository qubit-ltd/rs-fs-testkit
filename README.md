# Qubit FS Testkit

Reusable provider contract checks for implementations of
[`qubit-fs`](https://crates.io/crates/qubit-fs).

This crate is intended for development dependencies and keeps test support out
of the filesystem core dependency graph.
